.DS_Store
*.xcodeproj
DerivedData/
build/
xcuserdata/
*.xcuserstate
.swiftpm/
.build/
.superpowers/

# Owner-local catalog validator denylist (never commit real one)
Tools/owner_denylist.txt

# Raw authoring source (kept locally, never published)
Raw Data/
~$*

# Photo build output (full-size images staged for the content repo; thumbs are copied into Resources)
Tools/photo-build/
